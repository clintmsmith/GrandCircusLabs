function setup() {
    createCanvas(windowWidth, windowHeight);
}


// If you change the strokeWeight below, then it changes how large a circle is exposed
function draw() {
    strokeWeight(100);
    if (mouseIsPressed === true){
        line(mouseX, mouseY, pmouseX, pmouseY)
    }
}